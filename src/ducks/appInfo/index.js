import {
    getCurrentWordInfo,
    setUserLanguage,
    resetMainMode,
    selectMainMode,
    setModalSettings,
    clearModalSettings,
    setModalSelectedWords,
    clearModalSelectedWords
} from "./appInfoActions";

import appInfoReducer from "./appInfoReducer";

export {
    appInfoReducer,

    getCurrentWordInfo,
    setUserLanguage,
    resetMainMode,
    selectMainMode,
    setModalSettings,
    clearModalSettings,
    setModalSelectedWords,
    clearModalSelectedWords
};